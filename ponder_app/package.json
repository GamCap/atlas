{
  "name": "atlas",
  "private": true,
  "type": "module",
  "scripts": {
    "ponder": "node ./node_modules/@ponder/core/dist/bin/ponder.js",
    "dev": "pnpm run ponder dev",
    "start": "pnpm run ponder start",
    "codegen": "pnpm run ponder codegen",
    "lint": "eslint .",
    "typecheck": "tsc",
    "prepare": "git clone --branch feat/bigquery-sync-historical --single-branch https://github.com/erensanlier/ponder.git ../temp-ponder && cd ../temp-ponder && pnpm install && pnpm build && cd -"
  },
  "dependencies": {
    "@ponder/core": "link:../temp-ponder/packages/core",
    "hono": "^4.5.1",
    "viem": "^1.19.9"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.54.0",
    "eslint-config-ponder": "^0.4.2",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}